# Production Database (Neon)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require&channel_binding=require

# Server Port (MUST match frontend VITE_SOCKET_URL)
PORT=3000

# Environment
NODE_ENV=development

# Sentry Error Tracking
SENTRY_DSN=your_sentry_dsn_here

# Email Configuration (for verification and password reset)
# Get Gmail App Password: https://myaccount.google.com/apppasswords
# IMPORTANT: Remove all spaces from the app password (Gmail shows it with spaces but you must enter it without)
# Example: If Gmail shows "abcd efgh ijkl mnop", enter "abcdefghijklmnop"
# NOTE: Using port 465 (SSL) instead of 587 (STARTTLS) because Railway blocks port 587
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=jaffreonline@gmail.com
EMAIL_PASSWORD=xxxxxxxxxxxxxxxx
EMAIL_FROM=Jaffre <jaffreonline@gmail.com>
FRONTEND_URL=https://anthropic-joffre.vercel.app
